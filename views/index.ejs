<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AAYUSH-ADITI</title>
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>
     <div class="main-page">
        <div class="im_inside">
            <img src="/images/i5.jpg">
        </div>
        <div class="left">
            <% if(locals.name){ %>
                <img src="/images/img1.jpg">

            <% } else{ %>
                <img src="/images/love.png">
            <% } %>    
        </div>
        <div class="right">
            <% if(locals.name){ %>

                <h1 style="color: rgb(67, 42, 42); font-size: rem; font-weight: bolder;">Successfully Logged In</h1>
                <a href="/quiz">Love Quiz</a>
                
            <% } else{ %>
                
                <a href="/login">Login</a>
                <a href="/register">Register</a>
                
            <% } %>
        </div>
     </div>
</body>
</html>